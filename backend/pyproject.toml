[project]
name = "seoul-data-playground-backend"
version = "0.1.0"
description = "FastAPI backend for SEOUL DATA PLAYGROUND"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.28.0",
    "pydantic>=2.6.0",
    "pydantic-settings>=2.2.0",
    "sqlalchemy>=2.0.28",
    "psycopg2-binary>=2.9.9",
    "asyncpg>=0.29.0",
    "pytest>=8.1.0",
    "httpx>=0.27.0",
    "aiosqlite>=0.22.1",
    "greenlet>=3.5.5",
    "pandas>=2.3.3",
    "numpy>=2.2.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.4.0",
]
