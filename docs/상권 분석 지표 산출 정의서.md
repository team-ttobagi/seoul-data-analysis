# 상권 분석 지표 산출 정의서 (Version 1)

- **문서명**: 상권 분석 지표 산출 정의서
- **목적**: 상권 탐색 서비스의 데이터 및 지표 산출 기준 정의
- **데이터 기준**: PostgreSQL `sales_data`
- **문서 상태**: Published / Version 1
- **작성 기준일**: 2026-09-06

---

## 목차

1. [문서 개요](#1-문서-개요)
2. [데이터 구조 및 분석 기준](#2-데이터-구조-및-분석-기준)
3. [원천 데이터와 파생 데이터 구분](#3-원천-데이터와-파생-데이터-구분)
4. [핵심 지표 정의](#4-핵심-지표-정의)
   - [4.1 매출액](#41-매출액)
   - [4.2 거래건수](#42-거래건수)
   - [4.3 QoQ 매출 성장률](#43-qoq-매출-성장률)
   - [4.4 성장 점수](#44-성장-점수)
   - [4.5 거래 활성도 점수](#45-거래-활성도-점수)
   - [4.6 경쟁 점수 (CompetitionScore)](#46-경쟁-점수-competitionscore)
   - [4.7 최종 탐색 점수](#47-최종-탐색-점수)
5. [Benchmark Percentile 및 순위 산정](#5-benchmark-percentile-및-순위-산정)
6. [소비 패턴 분석](#6-소비-패턴-분석)
   - [6.1 시간대별 매출 비중](#61-시간대별-매출-비중)
   - [6.2 피크 시간대](#62-피크-시간대)
   - [6.3 요일별 매출 비중](#63-요일별-매출-비중)
   - [6.4 요일 평균 대비 증감률](#64-요일-평균-대비-증감률)
   - [6.5 피크 요일](#65-피크-요일)
   - [6.6 연령대별 매출 비중](#66-연령대별-매출-비중)
   - [6.7 주요 연령층](#67-주요-연령층)
   - [6.8 성별 매출 비중](#68-성별-매출-비중)
7. [Signal 및 Level 판정 기준](#7-signal-및-level-판정-기준)
8. [Insight / Warning 생성 기준](#8-insight--warning-생성-기준)
9. [Mock 데이터와 실제 DB 매핑](#9-mock-데이터와-실제-db-매핑)
10. [현재 데이터의 한계 및 제약사항](#10-현재-데이터의-한계-및-제약사항)
11. [전체 산식 요약](#11-전체-산식-요약)
12. [Python 구현 예시](#12-python-구현-예시)
13. [최종 데이터 처리 흐름](#13-최종-데이터-처리-흐름)
14. [핵심 산식 Quick Reference](#14-핵심-산식-quick-reference)
15. [추가 요구 사항](#15-추가-요구-사항)

---

# 1. 문서 개요

이 문서는 상권 탐색 서비스에서 사용하는 주요 데이터와 파생 지표의 정의, 산식, 정규화 방식, 비교 기준, 구현 제약을 하나의 기준으로 정리한 문서다.

대상 독자는 다음과 같다.

- **기획자**: 화면에 표시되는 값이 어떤 기준으로 만들어지는지 확인
- **개발자**: DB 조회와 서비스 로직 구현 기준 확인
- **데이터 분석 담당자**: 지표 정의와 정규화 방식 검토
- **발표/평가자**: 서비스 점수의 근거와 한계 이해

핵심 원칙은 다음과 같다.

1. 실제 DB에서 직접 조회할 수 있는 값과 계산이 필요한 파생 값을 구분한다.
2. 동일한 지표는 모든 화면에서 하나의 계산 결과를 재사용한다.
3. 비교 지표는 원칙적으로 **동일 분기 + 동일 업종의 상권 집단**을 기준으로 산출한다.
4. 점포 수가 없는 현재 DB 환경에서는 경쟁도를 직접 측정하지 않고, 매출·거래 데이터를 이용한 **경쟁 환경 Proxy**를 사용한다.
5. 모든 핵심 점수는 최종적으로 **높을수록 긍정적**인 방향으로 통일한다.

---

# 2. 데이터 구조 및 분석 기준

## 2.1 사용 테이블

### `trade_area_type`

| 컬럼             | 의미           |
| ---------------- | -------------- |
| `trdar_se_cd`    | 상권 구분 코드 |
| `trdar_se_cd_nm` | 상권 구분명    |

### `trade_area`

| 컬럼          | 의미           |
| ------------- | -------------- |
| `trdar_cd`    | 상권 코드      |
| `trdar_se_cd` | 상권 구분 코드 |
| `trdar_cd_nm` | 상권명         |

### `service_industry`

| 컬럼               | 의미             |
| ------------------ | ---------------- |
| `svc_induty_cd`    | 서비스 업종 코드 |
| `svc_induty_cd_nm` | 서비스 업종명    |

### `sales_data`

`sales_data`가 핵심 분석 테이블이다.

#### 기준 컬럼

- `stdr_yyqu_cd`
- `trdar_cd`
- `svc_induty_cd`

#### 전체 매출 / 거래

- `thsmon_selng_amt`
- `thsmon_selng_co`

#### 주중 / 주말

- `mdwk_selng_amt`
- `wkend_selng_amt`
- `mdwk_selng_co`
- `wkend_selng_co`

#### 요일별

- `mon_selng_amt`
- `tues_selng_amt`
- `wed_selng_amt`
- `thur_selng_amt`
- `fri_selng_amt`
- `sat_selng_amt`
- `sun_selng_amt`
- 각 요일별 `*_selng_co`

#### 시간대별

- `tmzon_00_06_selng_amt`
- `tmzon_06_11_selng_amt`
- `tmzon_11_14_selng_amt`
- `tmzon_14_17_selng_amt`
- `tmzon_17_21_selng_amt`
- `tmzon_21_24_selng_amt`
- 각 시간대별 `*_selng_co`

#### 성별

- `ml_selng_amt`
- `fml_selng_amt`
- `ml_selng_co`
- `fml_selng_co`

#### 연령별

- `agrde_10_selng_amt`
- `agrde_20_selng_amt`
- `agrde_30_selng_amt`
- `agrde_40_selng_amt`
- `agrde_50_selng_amt`
- `agrde_60_above_selng_amt`
- 각 연령대별 `*_selng_co`

## 2.2 기본 분석 단위

핵심 지표는 다음 단위로 산출한다.

```text
분기(stdr_yyqu_cd)
× 상권(trdar_cd)
× 업종(svc_induty_cd)
```

예를 들어 사용자가 `2026 Q2 / 커피·음료`를 조회한다면, 해당 분기의 해당 업종에 속한 서울 상권들을 서로 비교한다.

## 2.3 비교 집단 기준

정규화, percentile, rank는 원칙적으로 아래 집단을 사용한다.

```text
동일 stdr_yyqu_cd
+ 동일 svc_induty_cd
+ 서로 다른 trdar_cd
```

즉 업종 특성과 시점 차이를 최대한 제거한 상태에서 상권을 비교한다.

---

# 3. 원천 데이터와 파생 데이터 구분

| 구분             | 데이터 예시                           | 처리 방식        |
| ---------------- | ------------------------------------- | ---------------- |
| 원천 데이터      | `thsmon_selng_amt`, `thsmon_selng_co` | DB에서 직접 조회 |
| 단순 집계값      | 매출액, 거래건수                      | `SUM()`          |
| 시계열 파생값    | QoQ 매출 성장률, QoQ 거래 성장률      | 현재/전분기 비교 |
| 정규화 점수      | GrowthScore, TransactionScore         | 0~100 변환       |
| 복합 파생값      | CompetitionScore, ExplorationScore    | 여러 지표 가중합 |
| 패턴 분석값      | 피크 시간대, 피크 요일                | 비중/최댓값 계산 |
| 표현용 값        | `12.8억`, `45만`, `+12.4%`            | 포맷팅           |
| 규칙 기반 텍스트 | insight, warning                      | 점수 조건 템플릿 |

---

## 3.1 산식 표기 규칙

이 문서는 Markdown 뷰어마다 수식 렌더링이 달라지는 문제를 피하기 위해, 핵심 산식을 LaTeX보다 **고정폭 코드 블록**으로 표현한다.

산식은 가능한 한 다음 순서로 작성한다.

1. **정의**: 지표가 무엇을 의미하는지 설명
2. **원천 데이터**: 실제 사용하는 DB 컬럼 또는 선행 파생값
3. **산식**: 사람이 바로 읽을 수 있는 분수형 또는 줄바꿈형 식
4. **해석**: 값이 높거나 낮을 때 무엇을 의미하는지 설명
5. **예외 처리**: 0으로 나누기, 전분기 부재 등 필요한 경우 명시

예시:

```text
                         현재 분기 매출 - 이전 분기 매출
매출 성장률(%) = ───────────────────────────────── × 100
                                이전 분기 매출

GrowthRate
= (CurrentSales - PreviousSales)
  / PreviousSales
  × 100
```

복합 점수는 가중치가 한눈에 보이도록 세로로 정렬한다.

```text
ExplorationScore
= GrowthScore         × 0.40
+ TransactionScore    × 0.35
+ CompetitionScore    × 0.25
```

---

# 4. 핵심 지표 정의

## 4.1 매출액

### 정의

특정 분기·상권·업종에서 발생한 전체 매출 규모다.

### 원천 데이터

`thsmon_selng_amt`

### 산식

```text
Sales = SUM(thsmon_selng_amt)
```

### 해석

값이 클수록 해당 상권·업종의 현재 매출 규모가 크다.

---

## 4.2 거래건수

### 정의

특정 분기·상권·업종에서 발생한 전체 거래건수다.

### 원천 데이터

`thsmon_selng_co`

### 산식

```text
Transaction = SUM(thsmon_selng_co)
```

### 해석

매출액과는 별개로 실제 구매 행위가 얼마나 자주 발생했는지를 나타내는 규모 지표다.

---

## 4.3 QoQ 매출 성장률

### 정의

현재 분기 매출이 직전 분기 대비 얼마나 증가 또는 감소했는지를 나타낸다.

### 산식

```text
                         현재 분기 매출 - 이전 분기 매출
매출 성장률(%) = ───────────────────────────────── × 100
                                이전 분기 매출

GrowthRate
= (CurrentSales - PreviousSales)
  / PreviousSales
  × 100
```

### 예외 처리

- 전분기 데이터 없음 → `NULL`
- 전분기 매출이 0 → `NULL`
- 전분기 매출 > 0, 현재 매출 = 0 → `-100%`

### 주의

동일한 `qoq_growth_rate` 값을 KPI, Ranking, Compare, Why Explore, Exploration Score에서 함께 사용해야 한다.

현재 Mock처럼 성수동 성장률이 `8.2%`와 `12.4%`로 동시에 존재하면 안 된다.

---

## 4.4 성장 점수

### 정의

QoQ 매출 성장률을 동일 업종 내 상권끼리 비교할 수 있도록 0~100점으로 정규화한 값이다.

### 왜 단순 Min-Max를 쓰지 않는가

성장률은 낮은 기저효과 때문에 일부 상권이 매우 큰 양수 값을 가질 수 있다. 이 경우 단순 Min-Max를 사용하면 대부분 상권의 점수가 비슷하게 압축될 수 있다.

따라서 **5th percentile~95th percentile 범위로 극단값을 제한한 뒤 Min-Max 정규화**한다.

### 1단계: Winsorizing [1]

```text
ClippedGrowth
= clip(GrowthRate, P5, P95)
```

### 2단계: 0~100 정규화

```text
                 ClippedGrowth - P5
GrowthScore = ─────────────────────── × 100
                       P95 - P5
```

### 해석

- 100에 가까움 → 동일 업종 대비 성장성이 매우 높음
- 50 전후 → 비교군 중 중간 수준
- 0에 가까움 → 성장성이 낮음

[1] **Winsorizing**: 극단값이 전체 분포를 과도하게 왜곡하지 않도록 일정 기준을 벗어난 값을 경계값으로 제한하는 방식. 본 문서에서는 성장률을 하위 5%~상위 95% 범위로 제한한다.

---

## 4.5 거래 활성도 점수

### 정의

현재 분기 거래건수의 규모가 동일 업종의 다른 상권에 비해 얼마나 큰지를 나타낸다.

이 지표는 **거래 성장률이 아니라 현재 거래 규모**를 평가한다.

### 1단계: 로그 변환

```text
LogTransaction
= ln(1 + Transaction)
```

### 2단계: Min-Max 정규화

```text
                       LogTransaction - LogTransactionMin
TransactionScore = ───────────────────────────────────────── × 100
                       LogTransactionMax - LogTransactionMin
```

즉,

```text
                    ln(1 + Transaction) - ln(1 + TransactionMin)
TransactionScore = ─────────────────────────────────────────────── × 100
                    ln(1 + TransactionMax) - ln(1 + TransactionMin)
```

### 왜 로그를 사용하는가

거래건수는 초대형 상권과 일반 상권의 차이가 매우 클 수 있다. 로그 변환은 이 차이를 완화해 상권 간 상대 비교를 더 안정적으로 만든다.

### 해석

높을수록 거래가 활발한 상권이다.

---

## 4.6 경쟁 점수 (CompetitionScore)

### 정의

현재 DB에는 점포 수가 없기 때문에 실제 경쟁 업체 수를 직접 측정할 수 없다.

따라서 기존 Mock의 `competition` 필드와 `competition_score` 개념은 유지하되, **산출 근거만 재정의**한다.

> **CompetitionScore는 매출·거래 데이터를 기반으로 경쟁 환경을 간접 추정한 Proxy 점수이다.**

이 점수는 실제 점포 수나 경쟁사 수를 의미하지 않는다. 내부 계산에서는 `competition_score`를 그대로 사용하며, **높을수록 경쟁 환경이 상대적으로 긍정적**인 방향으로 통일한다.

### 구성

```text
CompetitionScore
= TicketScore          × 0.50
+ GrowthBalanceScore   × 0.30
+ DemandDiversityScore × 0.20
```

모든 구성요소는 **높을수록 긍정적**이다.

### 4.6.1 객단가 경쟁력 TicketScore — 50%

#### 객단가

```text
                  총 매출액
평균 객단가 = ───────────────
                  거래건수

AvgTicket
= Sales / Transaction
```

거래건수가 0이면 `NULL` 처리한다.

#### 정규화

동일 분기·동일 업종 내에서 객단가를 Min-Max 정규화한다.

```text
                 AvgTicket - AvgTicketMin
TicketScore = ───────────────────────────── × 100
               AvgTicketMax - AvgTicketMin
```

#### 해석

상대적으로 높은 객단가는 동일 수요 대비 가격 경쟁 압력이 덜할 가능성을 나타내는 간접 신호로 사용한다.

단, 낮은 객단가가 반드시 높은 경쟁을 의미하는 것은 아니다. 업종 특성, 상품 구성, 가격 전략 등의 영향도 받을 수 있다.

### 4.6.2 성장 균형 GrowthBalanceScore — 30%

#### 거래건수 QoQ 성장률

```text
                             현재 거래건수 - 이전 거래건수
거래 성장률(%) = ───────────────────────────────────── × 100
                                   이전 거래건수

TransactionGrowth
= (CurrentTransaction - PreviousTransaction)
  / PreviousTransaction
  × 100
```

#### 성장 압력

```text
GrowthPressure
= TransactionGrowth - SalesGrowth
```

거래가 크게 증가했지만 매출 증가가 상대적으로 약하면 평균 결제금액 하락 또는 가격 압력 가능성을 의심할 수 있다.

#### 정규화

먼저 `GrowthPressure`를 동일 분기·동일 업종 기준 0~1로 Min-Max 정규화한다.

```text
               GrowthPressure - PressureMin
PressureNorm = ───────────────────────────────
                 PressureMax - PressureMin
```

그다음 방향을 뒤집는다.

```text
GrowthBalanceScore
= (1 - PressureNorm) × 100
```

### 4.6.3 수요 다양성 DemandDiversityScore — 20%

상권 전체에서 업종별 매출 비중을 계산한다.

```text
                 업종 i 매출
업종 매출 비중 = ───────────────
                 상권 전체 매출

p_i
= IndustrySales_i / TotalTradeAreaSales
```

HHI:

```text
HHI
= Σ(p_i²)
```

수요 다양성:

```text
Diversity
= 1 - HHI
```

동일 분기 상권들의 Diversity를 Min-Max 정규화한다.

```text
DemandDiversityScore
= Norm(1 - HHI) × 100
```

### 해석

높을수록 특정 업종 하나에 소비가 지나치게 집중되지 않고 상권의 소비 기반이 다양하다고 본다.

### 한계

`CompetitionScore`는 경쟁을 직접 측정한 값이 아니다. 다음과 같은 다른 요인의 영향도 포함될 수 있다.

- 가격 전략
- 메뉴 구성
- 업종별 일반적인 객단가 구조
- 관광객 비중
- 이벤트성 소비
- 경기 변동

따라서 내부 데이터 필드명은 `competition_score`를 유지하되, 사용자 화면에서는 **경쟁 여건**처럼 더 이해하기 쉬운 표현으로 노출할 수 있다. 다만 문서에서는 실제 점포 수 기반 경쟁도가 아닌 **간접 추정 Proxy 점수**임을 명확히 표시한다.

---

## 4.7 최종 탐색 점수

### 정의

상권의 성장성, 거래 활성도, 시장 압력 안정성을 종합한 0~100점 점수다.

### 산식

```text
ExplorationScore
= GrowthScore         × 0.40
+ TransactionScore    × 0.35
+ CompetitionScore    × 0.25
```

### 예시

- GrowthScore = 91
- TransactionScore = 85
- CompetitionScore = 62

```text
ExplorationScore
= 91 × 0.40
+ 85 × 0.35
+ 62 × 0.25
= 81.65
```

화면 표시값은 반올림하여 `82점`으로 표현할 수 있다.

### 방향성

모든 하위 점수는 높은 값이 긍정적인 의미를 가지므로 단순 가중합이 가능하다.

---

# 5. Benchmark Percentile 및 순위 산정

## 5.1 Percentile 정의

사용자에게는 다음과 같이 표현한다.

```text
상위 N%
```

예:

- `5` → 상위 5%
- `18` → 상위 18%
- `38` → 상위 38%

따라서 percentile 값은 **낮을수록 상위권**이다.

## 5.2 비교 대상

항상 동일한:

- 분기
- 업종

의 상권 집단을 비교한다.

## 5.3 대상 지표

- Sales Percentile
- Growth Percentile
- Transaction Volume Percentile
- Market Pressure Percentile

## 5.4 산정 방법

내림차순 rank를 기준으로 상위 비율을 계산한다. **구현 기준(확정)**:

```text
                     내림차순 Rank
TopPercentile = ───────────────────── × 100
                  비교 대상 수(N)
```

값이 작을수록 상위권이다 (예: `TopPercentile = 3` → 상위 3%).

- 동점 처리: `RANK()`/`rank(method="min")` 방식 — 동일 값은 동일 순위를 받고, 다음 순위는 동점 개수만큼 건너뛴다.
- pandas 구현: `series.rank(method="min", pct=True, ascending=False) * 100` — `pct=True`는 정확히 `rank / N`을 의미하므로 위 공식과 동일하다.
- SQL 구현: `RANK() OVER (...) / COUNT(*) OVER () * 100`.
- **주의**: PostgreSQL의 `PERCENT_RANK()`는 `(rank - 1) / (N - 1)`로 위 공식과 다른 값을 낸다. 두 코드 경로(analytics 점수 파이프라인, `/sales/summary` 엔드포인트)가 서로 다른 산식을 쓰다 발견되어 전부 `RANK() / N × 100` 기준으로 통일했다. `PERCENT_RANK()`는 이 문서의 percentile 정의에는 사용하지 않는다.

### 5.5 서울 종합 순위 (`seoul_rank`)

`seoul_rank`는 동일 분기 및 동일 업종에 속한 서울 전체 상권을 대상으로, 주요 Benchmark 지표에서 전반적으로 우수한 상권을 판단하기 위한 종합 순위다.

종합 순위 산정에는 다음 4개 Percentile을 사용한다.

- Sales Percentile
- Growth Percentile
- Transaction Volume Percentile
- Market Pressure Percentile

각 Percentile은 값이 낮을수록 상위권을 의미하므로, 4개 Percentile의 평균값을 종합 Benchmark 값으로 사용한다.

```text
OverallBenchmarkPercentile
= (
    SalesPercentile
    + GrowthPercentile
    + TransactionVolumePercentile
    + MarketPressurePercentile
  ) / 4
```

`OverallBenchmarkPercentile`이 낮은 상권부터 순위를 부여한다.

```text
seoul_rank
= Rank(
    OverallBenchmarkPercentile,
    ascending=True
  )
```

비교 대상은 항상 동일한 분기(`stdr_yyqu_cd`)와 동일한 업종(`svc_induty_cd`)에 속한 서울 전체 상권으로 제한한다.

동점인 경우 `rank(method="min")` 방식을 적용한다.

예를 들어 다음과 같은 경우:

```text
상권 A
Sales Percentile              = 5
Growth Percentile             = 8
Transaction Volume Percentile = 4
Market Pressure Percentile    = 7

OverallBenchmarkPercentile
= (5 + 8 + 4 + 7) / 4
= 6
```

동일 분기·동일 업종 내에서 이 값이 가장 낮다면 `seoul_rank = 1`이 된다.

따라서 `seoul_rank = 1`은 매출, 성장성, 거래 활성도, 시장 부담 측면에서 종합적으로 가장 상위권에 위치한 상권을 의미한다.

---

# 6. 소비 패턴 분석

## 6.1 시간대별 매출 비중

### 산식

```text
                  해당 시간대 매출
시간대 비중(%) = ─────────────────── × 100
                  전체 시간대 매출

TimeShare_h
= TimeSales_h / TotalTimeSales × 100
```

### 사용 컬럼

- `tmzon_00_06_selng_amt`
- `tmzon_06_11_selng_amt`
- `tmzon_11_14_selng_amt`
- `tmzon_14_17_selng_amt`
- `tmzon_17_21_selng_amt`
- `tmzon_21_24_selng_amt`

---

## 6.2 피크 시간대

```text
PeakTime
= 매출액이 가장 큰 시간대
= argmax(TimeSales_h)
```

실제 DB 구간을 그대로 사용한다.

예를 들어 DB가 `17~21시`라면 Mock의 `18~22시`처럼 임의로 바꾸지 않는다.

---

## 6.3 요일별 매출 비중

```text
               해당 요일 매출
요일 비중(%) = ─────────────── × 100
                주간 전체 매출

DayShare_d
= DaySales_d / WeeklySalesTotal × 100
```

---

## 6.4 요일 평균 대비 증감률

### 일평균

```text
                   월~일 매출 합계
일평균 매출 = ───────────────────
                         7

AverageDailySales
= WeeklySalesTotal / 7
```

### 요일 평균 대비 증감

```text
DayDiff_d
= (DaySales_d - AverageDailySales)
  / AverageDailySales
  × 100
```

예를 들어 금요일이 평균보다 21% 높으면 `금요일 (+21%)`로 표현한다.

---

## 6.5 피크 요일

```text
PeakDay
= 매출액이 가장 큰 요일
= argmax(DaySales_d)
```

가장 매출이 높은 요일을 선택한다.

---

## 6.6 연령대별 매출 비중

```text
                  해당 연령대 매출
연령대 비중(%) = ───────────────── × 100
                  전체 연령대 매출

AgeShare_a
= AgeSales_a / TotalAgeSales × 100
```

사용 컬럼:

- `agrde_10_selng_amt`
- `agrde_20_selng_amt`
- `agrde_30_selng_amt`
- `agrde_40_selng_amt`
- `agrde_50_selng_amt`
- `agrde_60_above_selng_amt`

---

## 6.7 주요 연령층

```text
PrimaryAge
= 매출 비중이 가장 높은 연령대
= argmax(AgeSales_a)
```

예:

```text
20대 (45%)
```

현재 DB에는 연령×성별 교차값이 없으므로 `20대 여성 (45%)`처럼 결합해서 표현하면 안 된다.

---

## 6.8 성별 매출 비중

남성:

```text
                남성 매출
남성 비중(%) = ─────────────────── × 100
               남성 매출 + 여성 매출

MaleShare
= MaleSales / (MaleSales + FemaleSales) × 100
```

여성:

```text
                여성 매출
여성 비중(%) = ─────────────────── × 100
               남성 매출 + 여성 매출

FemaleShare
= FemaleSales / (MaleSales + FemaleSales) × 100
```

더 높은 쪽을 주요 성별로 표현할 수 있다.

---

# 7. Signal 및 Level 판정 기준

## 7.1 기본 점수 기준

|            점수 | Signal |
| --------------: | ------ |
|         70 이상 | High   |
| 40 이상 70 미만 | Medium |
|         40 미만 | Low    |

적용 대상:

- GrowthScore
- TransactionScore
- CompetitionScore

## 7.2 Percentile 기반 대안

데이터 분포가 한쪽으로 치우치는 경우 고정 점수 구간보다 percentile 구간이 더 안정적일 수 있다.

예:

| 상위 구간 | Signal |
| --------: | ------ |
|  상위 30% | High   |
|    30~70% | Medium |
|  하위 30% | Low    |

초기 버전에서는 70/40 기준으로 단순하게 운영하고, 실제 데이터 분포 확인 후 percentile 기반으로 전환할 수 있다.

---

# 8. Insight / Warning 생성 기준

Insight와 Warning은 원천 데이터가 아니라 **계산된 지표 조합을 문장으로 변환한 규칙 기반 결과**다.

예시 규칙:

```text
GrowthScore >= 70
AND TransactionScore >= 70
AND CompetitionScore < 40
```

출력:

> 성장성과 거래 활성도는 높지만 경쟁 여건이 좋지 않은 상권입니다.

다른 예:

```text
GrowthScore >= 70
AND TransactionScore < 70
AND CompetitionScore >= 70
```

출력:

> 성장세가 우수하고 시장 압력은 비교적 안정적이나 현재 거래 규모는 중간 수준입니다.

Insight/Warning은 LLM 자유 생성보다 초기에는 템플릿 기반이 일관성과 검증 측면에서 유리하다.

---

# 9. Mock 데이터와 실제 DB 매핑

| Mock 필드                             | 의미                | 실제 원천 데이터                    | 처리 방식             | 구현 가능 여부       |
| ------------------------------------- | ------------------- | ----------------------------------- | --------------------- | -------------------- |
| `trade_area_code`                     | 상권 코드           | `trade_area.trdar_cd`               | 직접 조회             | 가능                 |
| `trade_area_name`                     | 상권명              | `trade_area.trdar_cd_nm`            | 직접 조회             | 가능                 |
| `industry_code`                       | 업종 코드           | `service_industry.svc_induty_cd`    | 직접 조회             | 가능                 |
| `industry_name`                       | 업종명              | `service_industry.svc_induty_cd_nm` | 직접 조회             | 가능                 |
| `quarter`                             | 기준 분기           | `sales_data.stdr_yyqu_cd`           | 포맷팅                | 가능                 |
| `estimated_sales`                     | 매출                | `thsmon_selng_amt`                  | SUM                   | 가능                 |
| `estimated_sales_formatted`           | 표시 매출           | 매출액                              | 포맷팅                | 가능                 |
| `transaction_count`                   | 거래건수            | `thsmon_selng_co`                   | SUM                   | 가능                 |
| `transaction_count_formatted`         | 표시 거래건수       | 거래건수                            | 포맷팅                | 가능                 |
| `qoq_growth_rate`                     | QoQ 매출 성장률     | 현재/전분기 매출                    | 계산                  | 가능                 |
| `sales_growth.value`                  | 성장률 원본         | QoQ 매출 성장률                     | 재사용                | 가능                 |
| `sales_growth.normalized_score`       | 성장 점수           | GrowthRate                          | P5/P95 Clip + Min-Max | 가능                 |
| `growth_percentile`                   | 성장 상위 비율      | GrowthRate                          | Percentile            | 가능                 |
| `transaction_volume.value`            | 거래 규모           | `thsmon_selng_co`                   | SUM                   | 가능                 |
| `transaction_volume.normalized_score` | 거래 활성도         | 거래건수                            | Log + Min-Max         | 가능                 |
| `volume_percentile`                   | 거래량 상위 비율    | 거래건수                            | Percentile            | 가능                 |
| `sales_percentile`                    | 매출 상위 비율      | 매출                                | Percentile            | 가능                 |
| `seoul_rank`                          | 서울 순위           | 매출 또는 기획상 지정 지표          | Rank                  | 가능                 |
| `rankings.by_sales`                   | 매출 순위           | 매출                                | Rank                  | 가능                 |
| `rankings.by_volume`                  | 거래량 순위         | 거래건수                            | Rank                  | 가능                 |
| `rankings.by_growth`                  | 성장률 순위         | GrowthRate                          | Rank                  | 가능                 |
| `rankings.by_score`                   | 탐색 점수 순위      | ExplorationScore                    | Rank                  | 가능                 |
| `competition`                         | 경쟁 점수 Proxy     | 매출/거래/업종분포                  | 복합 계산             | 가능                 |
| `competition.normalized_score`        | 경쟁 점수           | Proxy 구성값                        | 가중합                | 가능                 |
| `store_count`                         | 점포 수             | 없음                                | -                     | 불가능               |
| `store_count_change`                  | 점포 증감           | 없음                                | -                     | 불가능               |
| `competition_level`                   | 실제 점포 경쟁 수준 | 점포 데이터 없음                    | 직접 산출 불가        | 불가능               |
| `signals.growth`                      | 성장 Signal         | GrowthScore                         | 구간화                | 가능                 |
| `signals.transaction`                 | 거래 Signal         | TransactionScore                    | 구간화                | 가능                 |
| `signals.competition`                 | 경쟁 Signal         | CompetitionScore                    | 구간화                | 가능, 명칭 변경 권장 |
| `when.slots[].percentage`             | 시간대 비중         | `tmzon_*_selng_amt`                 | 비중 계산             | 가능                 |
| `when.peak_slot`                      | 피크 시간대         | 시간대별 매출                       | MAX                   | 가능                 |
| `day.days[].percentage`               | 요일 비중           | 요일별 매출                         | 비중 계산             | 가능                 |
| `day.days[].diff_from_average`        | 평균 대비 증감      | 요일별 매출                         | 평균 대비 증감        | 가능                 |
| `day.peak_day`                        | 피크 요일           | 요일별 매출                         | MAX                   | 가능                 |
| `who.demographics[].percentage`       | 연령대 비중         | 연령별 매출                         | 비중 계산             | 가능                 |
| `strongest_age_group`                 | 주요 연령           | 연령별 매출                         | MAX                   | 가능                 |
| `20대 여성`                           | 연령×성별           | 없음                                | -                     | 불가능               |
| `district`                            | 자치구              | `district.signgu_cd`, `district.signgu_cd_nm` | FK JOIN / 필터·집계 | 가능                 |
| `exploration_score`                   | 최종 탐색 점수      | 3개 핵심 점수                       | 가중합                | 가능                 |
| `insight`                             | 요약 문구           | 파생 점수                           | 규칙 기반 생성        | 가능                 |
| `warning`                             | 주의 문구           | 파생 점수                           | 규칙 기반 생성        | 가능                 |

---

# 10. 현재 데이터의 한계 및 제약사항

## 10.1 점포 수 없음

현재 테이블에는 `store_count` 또는 개별 점포 데이터가 없다.

따라서:

- 점포 수
- 동일 업종 점포 수
- 점포 증감
- 점포 밀도

를 계산할 수 없다.

기존 `competition` 개념은 유지하되, 점포 수 기반 값이 아니라 현재 `sales_data`로 계산하는 `CompetitionScore` 추정 점수(proxy)로 재정의한다.

## 10.2 점포 증감 없음

분기별 점포 수 원천이 없으므로 `store_count_change`는 계산 불가다.

## 10.3 연령 × 성별 교차 데이터 없음

현재 DB에는:

- 연령별 매출
- 전체 남성 매출
- 전체 여성 매출

만 있다.

따라서 `20대 여성`, `30대 남성`처럼 연령과 성별을 결합한 실제 비율은 계산할 수 없다.

표현 예:

```text
주요 연령층: 20대 45%
주요 성별: 여성 61%
```

## 10.4 자치구 데이터 없음

`trade_area`에는 자치구 컬럼이 없으므로 `성동구`, `마포구` 등은 별도 행정구역 매핑 데이터가 필요하다.

## 10.5 CompetitionScore는 직접 경쟁도가 아님

객단가, 거래 성장과 매출 성장의 괴리, 소비 다양성은 경쟁 환경을 간접적으로 설명할 수 있지만 점포 수나 경쟁 업체 수를 직접 측정하지 않는다.

내부 필드명은 `competition_score`를 유지한다. 사용자 화면에서는 다음처럼 더 쉬운 표현을 권장한다.

> 경쟁 여건

단, 설명 문구에는 실제 경쟁업체 수가 아니라 매출·거래 데이터를 기반으로 한 간접 추정값임을 명시한다.

## 10.6 Mock 값과 실제 산식 결과 불일치 가능

기존 Mock은 화면 구현을 위한 예시값이다. 실제 데이터 적용 후 산식 결과가 Mock 숫자와 다르더라도 정상이다.

---

# 11. 전체 산식 요약

## 매출

```text
Sales = SUM(thsmon_selng_amt)
```

## 거래건수

```text
Transaction = SUM(thsmon_selng_co)
```

## QoQ 매출 성장률

```text
                         현재 분기 매출 - 이전 분기 매출
매출 성장률(%) = ───────────────────────────────── × 100
                                이전 분기 매출
```

## QoQ 거래 성장률

```text
                             현재 거래건수 - 이전 거래건수
거래 성장률(%) = ───────────────────────────────────── × 100
                                   이전 거래건수
```

## 성장 점수

```text
                 clip(GrowthRate, P5, P95) - P5
GrowthScore = ─────────────────────────────────── × 100
                          P95 - P5
```

## 거래 활성도 점수

```text
                    ln(1 + Transaction) - ln(1 + Tmin)
TransactionScore = ───────────────────────────────────── × 100
                    ln(1 + Tmax) - ln(1 + Tmin)
```

## 객단가

```text
AvgTicket = Sales / Transaction
```

## 성장 압력

```text
GrowthPressure = TransactionGrowth - SalesGrowth
```

## 성장 균형 점수

```text
GrowthBalanceScore
= (1 - Norm(GrowthPressure)) × 100
```

## 수요 다양성

```text
p_i = IndustrySales_i / TotalTradeAreaSales
```

```text
HHI = Σ(p_i²)
```

```text
Diversity = 1 - HHI
```

## 경쟁 점수

```text
CompetitionScore
= TicketScore          × 0.50
+ GrowthBalanceScore   × 0.30
+ DemandDiversityScore × 0.20
```

## 최종 탐색 점수

```text
ExplorationScore
= GrowthScore         × 0.40
+ TransactionScore    × 0.35
+ CompetitionScore    × 0.25
```

## 시간대 매출 비중

```text
TimeShare_h = TimeSales_h / TotalTimeSales × 100
```

## 요일 매출 비중

```text
DayShare_d = DaySales_d / WeeklySalesTotal × 100
```

## 요일 평균 대비 증감

```text
DayDiff_d
= (DaySales_d - AverageDailySales)
  / AverageDailySales
  × 100
```

## 연령대 매출 비중

```text
AgeShare_a = AgeSales_a / TotalAgeSales × 100
```

## 성별 매출 비중

```text
GenderShare_g
= GenderSales_g / (MaleSales + FemaleSales) × 100
```

---

# 12. Python 구현 예시

아래 코드는 PostgreSQL 조회 결과를 `pandas.DataFrame`으로 가져왔다는 가정으로 작성한다.

```python
import numpy as np
import pandas as pd


GROUP_KEYS = ["stdr_yyqu_cd", "trdar_cd", "svc_induty_cd"]
COMPARE_KEYS = ["stdr_yyqu_cd", "svc_induty_cd"]
SERIES_KEYS = ["trdar_cd", "svc_induty_cd"]

BENCHMARK_PERCENTILE_COLUMNS = [
    "sales_percentile",
    "growth_percentile",
    "volume_percentile",
    "competition_percentile",
]


def safe_minmax(series: pd.Series, neutral: float = 0.5) -> pd.Series:
    """Series를 0~1로 Min-Max 정규화한다."""
    valid = series.dropna()

    if valid.empty:
        return pd.Series(np.nan, index=series.index)

    min_value = valid.min()
    max_value = valid.max()

    if min_value == max_value:
        result = pd.Series(neutral, index=series.index, dtype=float)
        result[series.isna()] = np.nan
        return result

    return (series - min_value) / (max_value - min_value)


def aggregate_sales(df: pd.DataFrame) -> pd.DataFrame:
    """분기 × 상권 × 업종 단위로 매출과 거래건수를 집계한다."""
    return (
        df.groupby(GROUP_KEYS, as_index=False)
        .agg(
            sales=("thsmon_selng_amt", "sum"),
            transaction_count=("thsmon_selng_co", "sum"),
        )
    )


def add_qoq_growth(df: pd.DataFrame) -> pd.DataFrame:
    """QoQ 매출 성장률과 QoQ 거래건수 성장률을 계산한다."""
    result = df.copy()

    result = result.sort_values(
        ["trdar_cd", "svc_induty_cd", "stdr_yyqu_cd"]
    )

    result["previous_sales"] = (
        result.groupby(SERIES_KEYS)["sales"].shift(1)
    )
    result["previous_transaction_count"] = (
        result.groupby(SERIES_KEYS)["transaction_count"].shift(1)
    )

    result["growth_rate"] = np.where(
        result["previous_sales"] > 0,
        (
            (result["sales"] - result["previous_sales"])
            / result["previous_sales"]
            * 100
        ),
        np.nan,
    )

    result["transaction_growth"] = np.where(
        result["previous_transaction_count"] > 0,
        (
            (
                result["transaction_count"]
                - result["previous_transaction_count"]
            )
            / result["previous_transaction_count"]
            * 100
        ),
        np.nan,
    )

    return result


def growth_score(series: pd.Series) -> pd.Series:
    """성장률을 P5~P95로 제한한 후 0~100점으로 변환한다."""
    valid = series.dropna()

    if valid.empty:
        return pd.Series(np.nan, index=series.index)

    p5 = valid.quantile(0.05)
    p95 = valid.quantile(0.95)

    if p5 == p95:
        result = pd.Series(50.0, index=series.index)
        result[series.isna()] = np.nan
        return result

    clipped = series.clip(lower=p5, upper=p95)
    return ((clipped - p5) / (p95 - p5) * 100).clip(0, 100)


def transaction_score(series: pd.Series) -> pd.Series:
    """거래건수를 log1p 변환 후 0~100점으로 정규화한다."""
    log_values = np.log1p(series.clip(lower=0))
    return safe_minmax(log_values) * 100


def add_average_ticket(df: pd.DataFrame) -> pd.DataFrame:
    """평균 객단가를 계산한다."""
    result = df.copy()
    result["avg_ticket"] = (
        result["sales"]
        / result["transaction_count"].replace(0, np.nan)
    )
    return result


def ticket_score(series: pd.Series) -> pd.Series:
    """객단가를 동일 비교군 내 0~100점으로 정규화한다."""
    return safe_minmax(series) * 100


def add_growth_pressure(df: pd.DataFrame) -> pd.DataFrame:
    """거래 성장률과 매출 성장률 차이로 GrowthPressure를 계산한다."""
    result = df.copy()
    result["growth_pressure"] = (
        result["transaction_growth"] - result["growth_rate"]
    )
    return result


def growth_balance_score(series: pd.Series) -> pd.Series:
    """GrowthPressure가 높을수록 점수가 낮아지도록 역정규화한다."""
    normalized = safe_minmax(series)
    return (1 - normalized) * 100


def calculate_hhi(
    raw_df: pd.DataFrame,
    value_col: str = "thsmon_selng_amt",
) -> pd.DataFrame:
    """상권 전체 업종별 매출 비중으로 HHI와 Diversity(1-HHI)를 계산한다."""
    industry_sales = (
        raw_df.groupby(
            ["stdr_yyqu_cd", "trdar_cd", "svc_induty_cd"],
            as_index=False,
        )
        .agg(industry_sales=(value_col, "sum"))
    )

    industry_sales["total_area_sales"] = (
        industry_sales.groupby(
            ["stdr_yyqu_cd", "trdar_cd"]
        )["industry_sales"]
        .transform("sum")
    )

    industry_sales["share"] = np.where(
        industry_sales["total_area_sales"] > 0,
        industry_sales["industry_sales"]
        / industry_sales["total_area_sales"],
        np.nan,
    )

    industry_sales["share_sq"] = industry_sales["share"] ** 2

    hhi = (
        industry_sales.groupby(
            ["stdr_yyqu_cd", "trdar_cd"],
            as_index=False,
        )
        .agg(hhi=("share_sq", "sum"))
    )

    hhi["diversity"] = 1 - hhi["hhi"]
    return hhi


def add_demand_diversity_score(hhi_df: pd.DataFrame) -> pd.DataFrame:
    """동일 분기 상권들의 Diversity를 0~100점으로 정규화한다."""
    result = hhi_df.copy()
    result["demand_diversity_score"] = (
        result.groupby("stdr_yyqu_cd")["diversity"]
        .transform(safe_minmax)
        * 100
    )
    return result


def competition_score(
    ticket: pd.Series,
    growth_balance: pd.Series,
    diversity: pd.Series,
) -> pd.Series:
    """경쟁 점수 (CompetitionScore)를 계산한다. 높을수록 긍정적이다."""
    return (
        ticket * 0.50
        + growth_balance * 0.30
        + diversity * 0.20
    )


def exploration_score(
    growth: pd.Series,
    transaction: pd.Series,
    competition: pd.Series,
) -> pd.Series:
    """최종 탐색 점수를 계산한다."""
    return (
        growth * 0.40
        + transaction * 0.35
        + competition * 0.25
    )


def add_percentile_and_rank(
    df: pd.DataFrame,
    value_col: str,
    prefix: str,
) -> pd.DataFrame:
    """동일 분기·동일 업종 내 rank와 '상위 N%' percentile을 계산한다."""
    result = df.copy()

    result[f"{prefix}_rank"] = (
        result.groupby(COMPARE_KEYS)[value_col]
        .rank(method="min", ascending=False)
    )

    result[f"{prefix}_percentile"] = (
        result.groupby(COMPARE_KEYS)[value_col]
        .rank(method="min", pct=True, ascending=False)
        * 100
    )

    return result


def add_seoul_rank(
    df: pd.DataFrame,
    percentile_cols: list[str] = BENCHMARK_PERCENTILE_COLUMNS,
) -> pd.DataFrame:
    """Benchmark Percentile 평균을 기반으로 서울 종합 순위를 계산한다."""
    result = df.copy()

    result["overall_benchmark_percentile"] = (
        result[percentile_cols]
        .mean(axis=1, skipna=False)
    )

    result["seoul_rank"] = (
        result.groupby(COMPARE_KEYS)["overall_benchmark_percentile"]
        .rank(method="min", ascending=True)
    )

    return result


def calculate_time_patterns(df: pd.DataFrame) -> pd.DataFrame:
    """시간대별 매출 비중과 피크 시간대를 계산한다."""
    result = df.copy()

    columns = {
        "00-06시": "tmzon_00_06_selng_amt",
        "06-11시": "tmzon_06_11_selng_amt",
        "11-14시": "tmzon_11_14_selng_amt",
        "14-17시": "tmzon_14_17_selng_amt",
        "17-21시": "tmzon_17_21_selng_amt",
        "21-24시": "tmzon_21_24_selng_amt",
    }

    value_columns = list(columns.values())
    result["time_sales_total"] = result[value_columns].sum(axis=1)

    for label, column in columns.items():
        result[f"time_pct_{label}"] = np.where(
            result["time_sales_total"] > 0,
            result[column] / result["time_sales_total"] * 100,
            np.nan,
        )

    reverse_map = {v: k for k, v in columns.items()}
    peak_column = result[value_columns].idxmax(axis=1)
    result["peak_time"] = peak_column.map(reverse_map)

    return result


def calculate_day_patterns(df: pd.DataFrame) -> pd.DataFrame:
    """요일별 비중, 평균 대비 증감률, 피크 요일을 계산한다."""
    result = df.copy()

    columns = {
        "월요일": "mon_selng_amt",
        "화요일": "tues_selng_amt",
        "수요일": "wed_selng_amt",
        "목요일": "thur_selng_amt",
        "금요일": "fri_selng_amt",
        "토요일": "sat_selng_amt",
        "일요일": "sun_selng_amt",
    }

    value_columns = list(columns.values())
    result["weekly_sales_total"] = result[value_columns].sum(axis=1)
    result["average_daily_sales"] = result["weekly_sales_total"] / 7

    for label, column in columns.items():
        result[f"day_pct_{label}"] = np.where(
            result["weekly_sales_total"] > 0,
            result[column] / result["weekly_sales_total"] * 100,
            np.nan,
        )

        result[f"day_diff_{label}"] = np.where(
            result["average_daily_sales"] > 0,
            (
                result[column] - result["average_daily_sales"]
            )
            / result["average_daily_sales"]
            * 100,
            np.nan,
        )

    reverse_map = {v: k for k, v in columns.items()}
    peak_column = result[value_columns].idxmax(axis=1)
    result["peak_day"] = peak_column.map(reverse_map)

    return result


def calculate_age_patterns(df: pd.DataFrame) -> pd.DataFrame:
    """연령대별 매출 비중과 주요 연령층을 계산한다."""
    result = df.copy()

    columns = {
        "10대": "agrde_10_selng_amt",
        "20대": "agrde_20_selng_amt",
        "30대": "agrde_30_selng_amt",
        "40대": "agrde_40_selng_amt",
        "50대": "agrde_50_selng_amt",
        "60대 이상": "agrde_60_above_selng_amt",
    }

    value_columns = list(columns.values())
    result["age_sales_total"] = result[value_columns].sum(axis=1)

    for label, column in columns.items():
        result[f"age_pct_{label}"] = np.where(
            result["age_sales_total"] > 0,
            result[column] / result["age_sales_total"] * 100,
            np.nan,
        )

    reverse_map = {v: k for k, v in columns.items()}
    primary_column = result[value_columns].idxmax(axis=1)
    result["primary_age"] = primary_column.map(reverse_map)

    return result


def calculate_gender_patterns(df: pd.DataFrame) -> pd.DataFrame:
    """남녀 매출 비중과 주요 성별을 계산한다."""
    result = df.copy()

    gender_total = result["ml_selng_amt"] + result["fml_selng_amt"]

    result["male_share"] = np.where(
        gender_total > 0,
        result["ml_selng_amt"] / gender_total * 100,
        np.nan,
    )

    result["female_share"] = np.where(
        gender_total > 0,
        result["fml_selng_amt"] / gender_total * 100,
        np.nan,
    )

    result["dominant_gender"] = np.select(
        [
            result["female_share"] > result["male_share"],
            result["male_share"] > result["female_share"],
        ],
        ["female", "male"],
        default="same",
    )

    return result


def build_metric_dataset(raw_df: pd.DataFrame) -> pd.DataFrame:
    """핵심 상권 지표 데이터셋을 만드는 예시 파이프라인."""
    metrics = aggregate_sales(raw_df)
    metrics = add_qoq_growth(metrics)
    metrics = add_average_ticket(metrics)
    metrics = add_growth_pressure(metrics)

    metrics["growth_score"] = (
        metrics.groupby(COMPARE_KEYS)["growth_rate"]
        .transform(growth_score)
    )

    metrics["transaction_score"] = (
        metrics.groupby(COMPARE_KEYS)["transaction_count"]
        .transform(transaction_score)
    )

    metrics["ticket_score"] = (
        metrics.groupby(COMPARE_KEYS)["avg_ticket"]
        .transform(ticket_score)
    )

    metrics["growth_balance_score"] = (
        metrics.groupby(COMPARE_KEYS)["growth_pressure"]
        .transform(growth_balance_score)
    )

    hhi = calculate_hhi(raw_df)
    hhi = add_demand_diversity_score(hhi)

    metrics = metrics.merge(
        hhi[
            [
                "stdr_yyqu_cd",
                "trdar_cd",
                "hhi",
                "diversity",
                "demand_diversity_score",
            ]
        ],
        on=["stdr_yyqu_cd", "trdar_cd"],
        how="left",
    )

    metrics["competition_score"] = competition_score(
        metrics["ticket_score"],
        metrics["growth_balance_score"],
        metrics["demand_diversity_score"],
    )

    metrics["exploration_score"] = exploration_score(
        metrics["growth_score"],
        metrics["transaction_score"],
        metrics["competition_score"],
    )

    metrics = add_percentile_and_rank(
        metrics, "sales", "sales"
    )
    metrics = add_percentile_and_rank(
        metrics, "transaction_count", "volume"
    )
    metrics = add_percentile_and_rank(
        metrics, "growth_rate", "growth"
    )
    metrics = add_percentile_and_rank(
        metrics, "competition_score", "competition"
    )
    metrics = add_seoul_rank(metrics)
    metrics = add_percentile_and_rank(
        metrics, "exploration_score", "exploration"
    )

    return metrics


```

## Python 구현 시 주의사항

1. `stdr_yyqu_cd`가 문자열 정렬만으로 시간 순서가 보장되는 형식인지 확인해야 한다.
2. 전분기 데이터가 실제로 바로 이전 분기인지 검증하는 로직이 있으면 더 안전하다.
3. 정규화 대상이 1개뿐이거나 모든 값이 동일한 경우 중립값 50점을 사용하는 정책을 적용할 수 있다.
4. 결측값을 0점으로 자동 변환하지 않는다. 데이터 없음과 낮은 점수는 다른 의미다.
5. HHI 기반 Diversity는 특정 업종 하나가 아니라 상권 전체 업종 데이터를 이용해야 한다.
6. `CompetitionScore`는 실제 경쟁 점포 수가 아니라 간접 추정 Proxy이므로 내부 API 필드명은 `competition_score`를 유지할 수 있다. 사용자 화면에서는 `경쟁 여건`처럼 이해하기 쉬운 표현으로 노출하고, 간접 추정값이라는 설명을 함께 제공한다.

---

# 13. 최종 데이터 처리 흐름

```text
PostgreSQL sales_data
        │
        ├─ 분기 × 상권 × 업종 집계
        │      ├─ Sales
        │      └─ Transaction
        │
        ├─ 전분기 연결
        │      ├─ Sales Growth
        │      └─ Transaction Growth
        │
        ├─ 비교군 정규화
        │      ├─ GrowthScore
        │      ├─ TransactionScore
        │      ├─ TicketScore
        │      └─ GrowthBalanceScore
        │
        ├─ 상권 전체 업종 분포
        │      └─ HHI → DemandDiversityScore
        │
        ├─ CompetitionScore
        │      = Ticket 50%
        │      + GrowthBalance 30%
        │      + DemandDiversity 20%
        │
        ├─ ExplorationScore
        │      = Growth 40%
        │      + Transaction 35%
        │      + CompetitionScore 25%
        │
        ├─ Rank / Percentile
        │
        ├─ 소비 패턴
        │      ├─ 시간대
        │      ├─ 요일
        │      ├─ 연령
        │      └─ 성별
        │
        └─ API Response
               ├─ KPI
               ├─ Why Explore
               ├─ Rankings
               ├─ Compare
               ├─ Signals
               └─ Insight / Warning
```

### 구현 원칙

`GrowthRate`처럼 하나의 개념은 파이프라인에서 한 번 계산한 뒤 모든 화면에서 재사용한다.

```text
하나의 계산 결과
    ↓
KPI
Why Explore
Ranking
Compare
Exploration Score
```

화면별 repository 또는 service에서 별도의 숫자를 하드코딩하지 않는다.

---

# 14. 핵심 산식 Quick Reference

| 지표                 | 핵심 산식                                                | 방향                                   |
| -------------------- | -------------------------------------------------------- | -------------------------------------- |
| Sales                | `SUM(thsmon_selng_amt)`                                  | 클수록 매출 규모 큼                    |
| Transaction          | `SUM(thsmon_selng_co)`                                   | 클수록 거래 규모 큼                    |
| GrowthRate           | `(Sales_t - Sales_t-1) / Sales_t-1 × 100`                | 클수록 성장                            |
| TransactionGrowth    | `(T_t - T_t-1) / T_t-1 × 100`                            | 클수록 거래 증가                       |
| GrowthScore          | `100 × (clip(G,P5,P95)-P5)/(P95-P5)`                     | 높을수록 긍정적                        |
| TransactionScore     | `100 × [ln(1+T)-ln(1+Tmin)]/[ln(1+Tmax)-ln(1+Tmin)]`     | 높을수록 긍정적                        |
| AvgTicket            | `Sales / Transaction`                                    | 객단가                                 |
| TicketScore          | `Norm(AvgTicket) × 100`                                  | 높을수록 긍정적                        |
| GrowthPressure       | `TransactionGrowth - SalesGrowth`                        | 클수록 압력 가능성                     |
| GrowthBalanceScore   | `100 × (1 - Norm(GrowthPressure))`                       | 높을수록 긍정적                        |
| HHI                  | `Σ(p_i²)`                                                | 높을수록 집중                          |
| Diversity            | `1 - HHI`                                                | 높을수록 다양                          |
| DemandDiversityScore | `Norm(1-HHI) × 100`                                      | 높을수록 긍정적                        |
| CompetitionScore     | `0.50 Ticket + 0.30 GrowthBalance + 0.20 Diversity`      | 높을수록 경쟁 여건이 상대적으로 긍정적 |
| ExplorationScore     | `0.40 Growth + 0.35 Transaction + 0.25 CompetitionScore` | 높을수록 탐색 우선                     |
| TimeShare            | `TimeSales_h / ΣTimeSales × 100`                         | 시간대 비중                            |
| PeakTime             | `argmax(TimeSales_h)`                                    | 최대 시간대                            |
| DayShare             | `DaySales_d / ΣDaySales × 100`                           | 요일 비중                              |
| DayDiff              | `(DaySales_d - DailyAvg) / DailyAvg × 100`               | 평균 대비 증감                         |
| PeakDay              | `argmax(DaySales_d)`                                     | 최대 요일                              |
| AgeShare             | `AgeSales_a / ΣAgeSales × 100`                           | 연령 비중                              |
| PrimaryAge           | `argmax(AgeSales_a)`                                     | 주요 연령층                            |
| MaleShare            | `Male / (Male+Female) × 100`                             | 남성 비중                              |
| FemaleShare          | `Female / (Male+Female) × 100`                           | 여성 비중                              |

---

# 15 추가 요구 사항

Viwe 추가 관련

- MVP 이후 리팩토링 사항
- vw_trade_area_metrics : 일반 뷰, 단순 계산 정의를 한 곳에 모아놓음
- mv_trade_area_scores : 분기별 계산된 결과 저장 후 조회

```text
              외부 OpenAPI
                   │
                   ▼
           ┌──────────────┐
           │  sales_data  │
           │   원천 저장   │
           └──────┬───────┘
                  │
                  ▼
       ┌───────────────────────┐
       │ vw_trade_area_metrics │
       │                       │
       │ QoQ 매출 성장률       │
       │ QoQ 거래 성장률       │
       │ 객단가                │
       │ 성장 압력             │
       └───────────┬───────────┘
                   │
                   ▼
       ┌───────────────────────┐
       │ mv_trade_area_scores  │
       │                       │
       │ GrowthScore           │
       │ TransactionScore      │
       │ CompetitionScore      │
       │ ExplorationScore      │
       │ Percentile / Rank     │
       └───────────┬───────────┘
                   │
                   ▼
                Backend
                   │
                   ▼
          기존 고정 API Response
                   │
                   ▼
               Frontend
```

---

## 최종 정리

현재 PostgreSQL `sales_data`만으로 다음 핵심 기능은 구현 가능하다.

- 매출 규모
- 거래 규모
- QoQ 매출 성장률
- QoQ 거래 성장률
- 성장 점수
- 거래 활성도 점수
- 객단가
- 성장 균형
- HHI 기반 수요 다양성
- CompetitionScore Proxy
- 최종 Exploration Score
- 매출/거래/성장/점수 순위 및 percentile
- 시간대/요일/연령/성별 소비 패턴

현재 데이터만으로 구현할 수 없는 값은 다음과 같다.

- 실제 점포 수
- 점포 증감
- 실제 동일 업종 경쟁 업체 수
- 연령×성별 교차 집단 비율
- 자치구별 매출·거래 집계는 `district.signgu_cd`와 `trade_area.signgu_cd` FK JOIN으로 구현 가능

따라서 초기 서비스에서는 **실제 점포 경쟁도 대신 CompetitionScore Proxy를 사용하고, 해당 지표가 간접 추정값이라는 사실을 명확히 표기하는 방식**을 기준으로 한다.
